name: Desktop CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: desktop-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: ${{ matrix.name }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS arm64
            os: macos-15
            package_script: package:mac:dir
            smoke_server: smoke:packaged-server
            smoke_app: smoke:packaged-app
          - name: Windows x64
            os: windows-2025
            package_script: package:win:dir
            smoke_server: smoke:win:server
            smoke_app: smoke:win:app

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            renderer/package-lock.json
            server/package-lock.json
            electron/package-lock.json

      - name: Use pnpm 11.22.0
        uses: pnpm/action-setup@v4
        with:
          version: 11.22.0

      - name: Install
        run: npm ci --no-audit --no-fund
        env:
          # lock 文件生成自国内镜像(r2.cnpmjs.org)，其证书已过期且海外不可达；
          # 让 npm 在安装时把 resolved host 替换回官方 registry
          npm_config_replace_registry_host: registry.npmjs.org

      - name: Check native runtime
        run: npm run doctor

      - name: Audit production dependencies
        run: npm run audit:prod

      - name: Test full desktop stack
        if: runner.os == 'macOS'
        run: npm test

      - name: Test Windows renderer
        if: runner.os == 'Windows'
        run: npm --prefix renderer run typecheck && npm --prefix renderer test

      - name: Run deterministic UI Eval
        if: runner.os == 'macOS'
        run: npm run eval:pr -- --discard-failure-data

      - name: Upload Eval evidence
        if: always() && runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: eval-results-${{ matrix.name }}
          path: eval/results/
          if-no-files-found: ignore

      - name: Build unpacked app
        run: npm run ${{ matrix.package_script }}

      - name: Smoke packaged server
        run: npm --prefix electron run ${{ matrix.smoke_server }}

      - name: Smoke packaged app
        run: npm --prefix electron run ${{ matrix.smoke_app }}
