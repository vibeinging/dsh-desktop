# DSH 0.1.1 uses Cordis service peers across the assembled Profile graph.
# npm's strict peer solver does not terminate on that cyclic graph, while the
# official lockfile-selected packages are verified by the runtime Profile tests.
legacy-peer-deps=true
