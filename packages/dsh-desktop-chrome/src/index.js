/** Host loader entry for the browser-only desktop chrome plugin. */

export const name = "dsh-desktop-chrome";
export const inject = [];

/** The desktop title bar has no Host-side behavior. */
export function apply() {}
