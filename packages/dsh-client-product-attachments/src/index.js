/** Profile Bundle marker for the product attachment presentation plugin. */
export const name = 'dsh-client-product-attachments'

/** The host half exists so the Profile row owns the browser plugin lifecycle. */
export function apply() {}
