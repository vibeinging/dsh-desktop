/** Host-side marker for the desktop product search-mode Client Bundle. */

export const name = "dsh-client-product-search-mode";

/** The host half exists only so the same Profile row can carry a browser Client Plugin. */
export function apply() {}
