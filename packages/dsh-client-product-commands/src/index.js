/** Host-side marker for the desktop product command Client Bundle. */

export const name = "dsh-client-product-commands";

/** The host half exists only so the same Profile row can carry a browser Client Plugin. */
export function apply() {}
