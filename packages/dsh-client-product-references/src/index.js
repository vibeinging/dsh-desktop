/** Host-side marker for the desktop product reference Client Bundle. */

export const name = "dsh-client-product-references";

/** The host half exists only so the same Profile row can carry a browser Client Plugin. */
export function apply() {}
